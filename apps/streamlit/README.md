# Optional Streamlit UI (legacy)

Uses the same OpenAI + Agno agents in a simpler Streamlit interface.

```bash
cd apps/streamlit
source ../../.venv/bin/activate
pip install -r requirements.txt
streamlit run health_agent.py
```
